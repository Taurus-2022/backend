
### 心跳包
GET {{baseUrl}}/ping

### 创建签名
POST {{baseUrl}}/signatures

# 响应示例：
# success
# 创建请求不返回任何值，请根据状态码判断是否成功

### 获取签名人数
GET {{baseUrl}}/signatures/count?street="大冲街道"

# 响应示例：
#{
#    "count": 100
#}